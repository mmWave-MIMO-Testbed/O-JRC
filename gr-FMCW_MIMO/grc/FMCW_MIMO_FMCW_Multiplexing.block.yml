id: FMCW_MIMO_FMCW_Multiplexing
label: FMCW_Multiplexing
category: '[FMCW_MIMO]'

templates:
  imports: import FMCW_MIMO
  make: FMCW_MIMO.FMCW_Multiplexing(${N_tx},${multiplexing_type},${TDM_delay},${len_key})

#  Make one 'parameters' list entry for every parameter you want settable from the GUI.
#     Keys include:
#     * id (makes the value accessible as \$keyname, e.g. in the make entry)
#     * label (label shown in the GUI)
#     * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
parameters:
- id: N_tx
  label: Number of TX
  dtype: int
  default: 1
- id: multiplexing_type
  label: Multiplexing Type
  dtype: bool
  default: '"TDM"'
  options: ['"TDM"', '"FDM"']
- id: TDM_delay
  label: TDM TX Delay Time (ms)
  dtype: float
  default: 10
- id: len_key
  label: Packet Length Key
  dtype: string
  default: '"packet_len"'
  hide: part


#  Make one 'inputs' list entry per input and one 'outputs' list entry per output.
#  Keys include:
#      * label (an identifier for the GUI)
#      * domain (optional - stream or message. Default is stream)
#      * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
#      * vlen (optional - data stream vector length. Default is 1)
#      * optional (optional - set to 1 for optional inputs. Default is 0)
inputs:
-   domain: stream
    label: tx_in
    dtype: complex
    multiplicity: ${N_tx}
-   domain: stream
    label: FMCW_in
    dtype: complex
    multiplicity: ${1}

outputs:
-   domain: stream
    label: tx_out
    dtype: complex
    multiplicity: ${N_tx}

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
